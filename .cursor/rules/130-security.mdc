---
description: Security conventions for API routes, webhooks, and secrets
globs: src/routes/** src/middleware/**
alwaysApply: false
---

# Security Conventions

## Authentication
- All user-facing Express routes must use the Clerk auth middleware
- Never trust client-provided user IDs — always derive from the authenticated session
- Admin routes must verify against an admin allow-list

## Webhooks
- Clerk webhook route: verify the `svix` signature using `CLERK_WEBHOOK_SECRET`
- External service webhooks: validate expected fields and verify signatures when possible
- Never expose internal data in webhook responses — return minimal `{ ok: true }` or `{ error: "..." }`

## Secrets & Environment
- Never hardcode API keys, secrets, or connection strings in source code
- **Local dev**: secrets go in `.env` (git-ignored, never committed)
- **Production**: secrets go in Railway environment variables via the dashboard
- Never log secrets or include them in error messages
- Keep `.env.example` in sync with all required variables (without values)

## Data Access
- Always scope DB queries to the authenticated `userId` for user-owned resources
- Use `findFirst` with ownership check instead of `findUnique` by ID alone

## Forbidden
- Never commit `.env` or any file with real secrets
- Never expose raw database errors to the client
- Never use `eval()`, `Function()`, or dynamic code execution
- Never trust or execute user-provided code
- Never disable TypeScript strict mode
